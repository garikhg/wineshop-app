import Head from 'next/head';const SEOHead = () => {    const title = 'Wine Shop';    return (        <Head>            <meta property="og:type" content="website"/>            <meta property="twitter:card" content="summary_large_image"/>            { title && (                <>                    <title>{ title }</title>                </>            ) }        </Head>    );}export default SEOHead;